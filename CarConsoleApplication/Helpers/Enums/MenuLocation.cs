namespace CarConsoleApplication.Helpers.Enums
{
    //bu enum menü itemlarının db'ye kolay yansıtılması ve okunması için var.
    public enum MenuLocation
    {
        TabBar = 1,
        SettingsSidebar = 2
    }
}
